# jetson-nano-communication

### Run as client

- Create directory: client/build
- Run on terminal:

```
cd client/build
cmake ..
make run
```

## Run as server

- Create directory: server/build
- Run on terminal:

```
cd server/build
cmake ..
make run
```
